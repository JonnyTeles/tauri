{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-capability",
  "description": "Permite abrir links externos no navegador padrão",
  "windows": ["*"],
  "permissions": ["shell:default", "shell:allow-open"],
  "remote": {
    "urls": [
      "https://*.twitch.tv",
      "https://*.youtube.com",
      "https://*.google.com",
      "https://7tv.app",
      "https://*.7tv.app"
    ],
    "permissions": ["core:shell:allow-open"]
  }
}
